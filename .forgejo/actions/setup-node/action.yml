# action.yml
name: setup-node
description: 'setup node'

runs:
  using: 'composite'
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version-file: .node-version
        cache: 'npm'
    - shell: bash
      run: npm ci
